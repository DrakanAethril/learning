<h3 class="mb-0 flex">
    <div class="pt-1" style="float:left">{{'resource.trainings-title'|trans}}</div>
    <div class="text-end mr-1"><a href="#" class="btn btn-secondary">New</a></div>
</h3>
<div class="divider mt-0 mb-0">
    <div class="divider-text">
        <i class="bx bx-star"></i>
    </div>
</div>
<div class="table-responsive text-nowrap">
    <table class="table table-hover">
        <thead>
            <tr>
                <th>{{'training.name'|trans}}</th>
                <!--<th>{{'training.status'|trans}}</th>-->
                <th>{{'actions'|trans}}</th>
            </tr>
        </thead>
        <tbody class="table-border-bottom-0 mb-5">
            {% if resource.trainings is not empty %}
                {% for training in resource.trainings %}
                    <tr>
                        <td>
                            <i class="fab fa-angular fa-lg text-danger me-3"></i>
                            <strong>{{ training.name }}</strong>
                        </td>
                        <!--
                        <td>
                            <span class="badge bg-label-primary me-1">{{ resource.status.name }}</span>
                        </td>
                        -->
                        <td>
                            <a href="{{ path('resources_create', {id: resource.id}) }}" class="btn btn-icon btn-outline-primary rounded-pill bx bx-edit-alt me-1"></a>
                            <!--<div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                    <i class="bx bx-dots-vertical-rounded"></i>
                                </button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" href="{{ path('resources_create', {id: resource.id}) }}">
                                        <i class="bx bx-edit-alt me-1"></i>
                                        Edit</a>
                                    <a class="dropdown-item" href="javascript:void(0);">
                                        <i class="bx bx-trash me-1"></i>
                                        Delete</a>
                                </div>
                            </div>-->
                        </td>
                    </tr>
                {% endfor %}
            {% endif %}
        </tbody>
    </table>
</div>